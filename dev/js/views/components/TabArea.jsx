import React from 'react/addons';
import Radium from 'radium';
import Vendor from 'react-vendor-prefix';

var styles = Vendor.prefix({
});

class TabArea extends React.Component {
}

export default Radium(TabArea);
